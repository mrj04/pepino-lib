'use strict';

module.exports = {
  isTruthy: function isTruthy(variable) {
    return variable && variable !== 'false' && variable !== '';
  },
  isFalsey: function isFalsey(variable) {
    return !variable || variable === 'false' || variable === '';
  }
};